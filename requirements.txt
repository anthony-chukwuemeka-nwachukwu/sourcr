# Core framework: agents, tasks, and flows.
# [anthropic] adds the native Anthropic provider (the brief agent runs on
# Claude). OpenAI works out of the box; add other provider extras as needed.
crewai[anthropic]>=1.14.7,<2.0.0

# Prebuilt tools, including SerperDevTool for web search
crewai-tools>=0.17.0

# Tavily client — required by crewai-tools' TavilySearchTool (Profiler verification)
tavily-python>=0.5.0

# Typed data contracts passed between crews
pydantic>=2.0.0

# Pipeline-level confidence chart (headless Agg backend)
matplotlib>=3.8.0

# Loads ANTHROPIC_API_KEY / SERPER_API_KEY from .env
python-dotenv>=1.0.0

#.venv\Scripts\Activate.ps1
