# Development / test dependencies (not needed to run the pipeline).
# Install with:  pip install -r requirements-dev.txt
pytest>=8.0.0
# Used by FastAPI's TestClient in the backend tests
httpx>=0.27.0
